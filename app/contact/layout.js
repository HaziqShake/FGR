export const metadata = {
  title: 'Contact | FitCheck',
  description: 'Get in touch with the developer behind FitCheck.',
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
