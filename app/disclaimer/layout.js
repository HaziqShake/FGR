export const metadata = {
  title: 'Disclaimer | FitCheck',
  description: 'Legal disclaimer for FitCheck — an independent hardware compatibility tool.',
};

export default function DisclaimerLayout({ children }) {
  return <>{children}</>;
}
